import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import { detectDeviceLanguage, isSearchCrawler, languageHomepage, parseAcceptLanguageHeader, resolveFirstVisitLanguage } from "../lib/i18n/firstVisitLanguage.mjs";
const read = (path) => readFile(new URL(`../${path}`, import.meta.url), "utf8");
test("saved selection wins on first visit", () => { assert.equal(resolveFirstVisitLanguage({ pathname: "/", savedLanguage: "tr", navigatorLanguages: ["en-US"], userAgent: "Mozilla/5.0" }), "tr"); assert.equal(resolveFirstVisitLanguage({ pathname: "/", savedLanguage: "ar", navigatorLanguages: ["en-US"], userAgent: "Mozilla/5.0" }), "ar"); });
test("explicit localized URLs and deep links never redirect", () => { assert.equal(resolveFirstVisitLanguage({ pathname: "/en", savedLanguage: "tr", navigatorLanguages: ["tr-TR"], userAgent: "Mozilla/5.0" }), null); assert.equal(resolveFirstVisitLanguage({ pathname: "/tr/blog", navigatorLanguages: ["en-US"], userAgent: "Mozilla/5.0" }), null); assert.equal(resolveFirstVisitLanguage({ pathname: "/contact", navigatorLanguages: ["tr-TR"], userAgent: "Mozilla/5.0" }), null); });
test("Accept-Language quality order selects a supported language", () => { const languages = parseAcceptLanguageHeader("de-DE;q=0.7, tr-TR;q=0.8, en-US;q=0.9, ar;q=0.4"); assert.deepEqual(languages, ["en-US", "tr-TR", "de-DE", "ar"]); assert.equal(detectDeviceLanguage(languages), "en"); });
test("unsupported device language defaults to Arabic", () => { assert.equal(detectDeviceLanguage(["fr-FR"]), "ar"); assert.equal(languageHomepage("ar"), "/"); });
test("crawlers and preview agents are excluded", () => { for (const userAgent of ["Googlebot/2.1", "bingbot/2.0", "Lighthouse", "Twitterbot/1.0"]) { assert.equal(isSearchCrawler(userAgent), true); assert.equal(resolveFirstVisitLanguage({ pathname: "/", navigatorLanguages: ["en-US"], userAgent }), null); } });
test("middleware redirects before rendering without loops", async () => { const middleware = await read("middleware.ts"); for (const token of ["resolveFirstVisitRedirect", "parseAcceptLanguageHeader", "SITE_LANGUAGE_STORAGE_KEY", 'request.nextUrl.pathname !== "/"', 'language !== "en" && language !== "tr"', "NextResponse.redirect(redirectUrl, 307)"]) assert.ok(middleware.includes(token), token); });
