name: HAMZA AGENCY Closeout Dispatcher

on:
  workflow_dispatch:
    inputs:
      execution_mode:
        description: Safe execution mode
        required: true
        type: choice
        options: [preview-readonly, local-isolated, production-readonly]
      suite:
        description: Independent suite
        required: true
        type: choice
        options: [public, translations, tracking, admin, page-builder, backup-restore, trash, notifications, permissions, security]
      target_url:
        description: Exact target URL
        required: true
        type: string
      expected_sha:
        description: Full 40-character PR Head SHA
        required: true
        type: string
      collect_only:
        description: Collect existing evidence without running tests
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  validate-inputs:
    runs-on: ubuntu-latest
    outputs:
      allowed: ${{ steps.guard.outputs.allowed }}
    steps:
      - id: guard
        shell: bash
        run: |
          set -euo pipefail
          sha='${{ inputs.expected_sha }}'
          [[ "$sha" =~ ^[0-9a-fA-F]{40}$ ]]
          mode='${{ inputs.execution_mode }}'
          suite='${{ inputs.suite }}'
          url='${{ inputs.target_url }}'
          case "$mode" in
            preview-readonly) [[ "$url" != https://hamza-agency.com* && "$url" != https://www.hamza-agency.com* ]] ;;
            production-readonly) [[ "$url" == https://hamza-agency.com* || "$url" == https://www.hamza-agency.com* ]] ;;
            local-isolated) [[ "$url" == http://127.0.0.1:* || "$url" == http://localhost:* ]] ;;
            *) exit 1 ;;
          esac
          case "$suite" in
            tracking|admin|page-builder|backup-restore|trash|notifications|permissions)
              test "$mode" = local-isolated ;;
          esac
          echo "allowed=true" >> "$GITHUB_OUTPUT"

  run-suite:
    needs: validate-inputs
    if: needs.validate-inputs.outputs.allowed == 'true'
    uses: ./.github/workflows/hamza-closeout-suite.yml
    with:
      execution_mode: ${{ inputs.execution_mode }}
      suite: ${{ inputs.suite }}
      target_url: ${{ inputs.target_url }}
      expected_sha: ${{ inputs.expected_sha }}
      collect_only: ${{ inputs.collect_only }}
