# HAMZA AGENCY Pending Tasks

## Not completed yet

- Add `/ai-support` link to `components/PublicQuickNav.tsx`.
- Add `/ai-support` section link to `app/digital-services/page.tsx`.
- Add metadata file for `app/partners/layout.tsx`.
- Improve fallback review wording in `app/reviews/page.tsx`.
- Add metadata files for legal pages:
  - `app/privacy-policy/layout.tsx`
  - `app/terms-and-conditions/layout.tsx`
  - `app/ai-policy/layout.tsx`

## Manual checks still required

- Test admin add/edit/show/hide/delete flows.
- Test Supabase RLS and access rules from Supabase dashboard.
- Create and download a real backup from `/admin/backups`.
- Test public links on mobile.
- Final SEO and Google Search Console check.
