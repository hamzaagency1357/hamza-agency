# HAMZA AGENCY Pending Tasks

## Completed from this tracker

- Added `/ai-support` link to `components/PublicQuickNav.tsx`.
- Added metadata file for `app/privacy-policy/layout.tsx`.
- Reduced homepage header navigation clutter through `app/final-fixes.css`.
- Hid the floating public quick navigation button for final launch through `app/final-fixes.css`.
- Added image performance settings in `next.config.ts`.

## Not completed yet

- Add `/ai-support` section link to `app/digital-services/page.tsx`.
- Add metadata file for `app/partners/layout.tsx`.
- Improve fallback review wording in `app/reviews/page.tsx`.
- Add remaining metadata files for legal pages:
  - `app/terms-and-conditions/layout.tsx`
  - `app/ai-policy/layout.tsx`

## External or manual checks still required

- Create official domain email from the domain/email provider.
- Test admin add/edit/show/hide/delete flows.
- Test Supabase RLS and access rules from Supabase dashboard.
- Create and download a real backup from `/admin/backups`.
- Test public links on mobile.
- Final SEO and Google Search Console check.
