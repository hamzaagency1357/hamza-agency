"use client";


import { adminBoundaryMutation } from "@/lib/adminBoundaryMutationClient";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { requireAdminModuleAccess } from "@/lib/adminAccess";
import { supabase } from "@/lib/supabase";

type LanguageCode = "en" | "tr";
type FieldName = "title" | "summary" | "content" | "button_label";
type GalleryItem = { id: string; title: string; category: string; description: string; buttonLabel: string; isVisible: boolean; status: string };
type TranslationRow = { source_id: string | number | null; field_name: string | null; language: string | null; translated_value: string | null; reviewed: boolean | null; is_published: boolean | null; status: string | null };
type TranslationState = { values: Partial<Record<FieldName, string>>; reviewed: boolean; published: boolean };
type TranslationPack = Record<string, Partial<Record<LanguageCode, TranslationState>>>;
type GenericRow = Record<string, unknown>;

const languages: Array<{ code: LanguageCode; label: string }> = [{ code: "en", label: "English" }, { code: "tr", label: "Türkçe" }];
const fields: Array<{ key: FieldName; label: string; helper: string }> = [
  { key: "title", label: "عنوان العنصر", helper: "العنوان الظاهر في بطاقة المعرض." },
  { key: "summary", label: "التصنيف", helper: "الشارة الظاهرة فوق الصورة أو المشهد." },
  { key: "content", label: "الوصف", helper: "النص الظاهر تحت العنوان." },
  { key: "button_label", label: "زر الإجراء", helper: "نص الزر فقط؛ الرابط نفسه لا يتغير." },
];
function text(row: GenericRow, key: string, fallback = "") { const value = row[key]; return typeof value === "string" ? value.trim() || fallback : typeof value === "number" ? String(value) : fallback; }
function emptyState(): TranslationState { return { values: {}, reviewed: false, published: false }; }
function packKey(id: string) { return `gallery_items:${id}`; }
function isField(value: string | null): value is FieldName { return fields.some((field) => field.key === value); }
function sourceValue(item: GalleryItem, field: FieldName) { if (field === "title") return item.title; if (field === "summary") return item.category; if (field === "content") return item.description; return item.buttonLabel; }
function activeFields(item: GalleryItem) { return fields.filter((field) => Boolean(sourceValue(item, field.key).trim())); }
function complete(item: GalleryItem, state: TranslationState) { const active = activeFields(item); return active.length > 0 && active.every((field) => Boolean(state.values[field.key]?.trim())); }
function progress(item: GalleryItem, state: TranslationState) { const active = activeFields(item); return active.length ? Math.round((active.filter((field) => Boolean(state.values[field.key]?.trim())).length / active.length) * 100) : 0; }
function buildPack(items: GalleryItem[], rows: TranslationRow[]): TranslationPack { const next: TranslationPack = {}; items.forEach((item) => { const key = packKey(item.id); next[key] = {}; languages.forEach(({ code }) => { const matches = rows.filter((row) => String(row.source_id ?? "") === item.id && row.language === code && isField(row.field_name)); const values: Partial<Record<FieldName, string>> = {}; matches.forEach((row) => { if (isField(row.field_name)) values[row.field_name] = row.translated_value || ""; }); const active = activeFields(item); const reviewed = active.length > 0 && active.every((field) => Boolean(matches.find((row) => row.field_name === field.key)?.reviewed)); const published = reviewed && active.every((field) => { const row = matches.find((match) => match.field_name === field.key); return Boolean(row?.is_published || row?.status === "published"); }); next[key][code] = { values, reviewed, published }; }); }); return next; }

export default function GalleryTranslationsPage() {
  const router = useRouter();
  const [authorized, setAuthorized] = useState(false); const [checking, setChecking] = useState(true); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [adminEmail, setAdminEmail] = useState(""); const [items, setItems] = useState<GalleryItem[]>([]); const [pack, setPack] = useState<TranslationPack>({}); const [selectedId, setSelectedId] = useState(""); const [language, setLanguage] = useState<LanguageCode>("en"); const [message, setMessage] = useState(""); const [error, setError] = useState("");
  useEffect(() => { async function checkAccess() { const access = await requireAdminModuleAccess("gallery"); if (!access.isAuthorized || !access.profile) { router.replace(access.reason === "forbidden" ? "/admin" : "/admin/login"); setChecking(false); return; } setAdminEmail(access.profile.email || access.user?.email || ""); setAuthorized(true); setChecking(false); } void checkAccess(); }, [router]);
  useEffect(() => { if (authorized) void loadContent(); }, [authorized]);
  async function loadContent() { const client = supabase; if (!client) { setError("الاتصال بقاعدة البيانات غير مفعل."); setLoading(false); return; } setLoading(true); setMessage(""); setError(""); const [itemsResult, translationsResult] = await Promise.all([client.from("gallery_items").select("id, title, category, description, button_label, is_visible, status").order("sort_order", { ascending: true }).limit(300), client.from("content_translations").select("source_id, field_name, language, translated_value, status, reviewed, is_published").eq("source_type", "gallery_items").in("field_name", fields.map((field) => field.key)).in("language", ["en", "tr"]).limit(3000)]); setLoading(false); if (itemsResult.error) { setError(`تعذر تحميل عناصر المعرض: ${itemsResult.error.message}`); return; } const loaded = ((itemsResult.data || []) as GenericRow[]).map((row, index) => ({ id: text(row, "id", `gallery-${index}`), title: text(row, "title", "عنصر بدون عنوان"), category: text(row, "category"), description: text(row, "description"), buttonLabel: text(row, "button_label"), isVisible: row.is_visible !== false, status: text(row, "status", "published") })); setItems(loaded); setPack(buildPack(loaded, (translationsResult.data || []) as TranslationRow[])); setSelectedId((current) => current || loaded[0]?.id || ""); if (translationsResult.error) setError(`تم تحميل العناصر، لكن تعذر تحميل ترجماتها: ${translationsResult.error.message}`); }
  const selectedItem = items.find((item) => item.id === selectedId) || items[0] || null; const selectedKey = selectedItem ? packKey(selectedItem.id) : ""; const state = selectedKey ? pack[selectedKey]?.[language] || emptyState() : emptyState(); const isComplete = selectedItem ? complete(selectedItem, state) : false; const reviewedCount = useMemo(() => items.filter((item) => { const itemState = pack[packKey(item.id)]?.[language] || emptyState(); return itemState.reviewed && complete(item, itemState); }).length, [items, pack, language]); const publishedCount = useMemo(() => items.filter((item) => { const itemState = pack[packKey(item.id)]?.[language] || emptyState(); return itemState.published && itemState.reviewed && complete(item, itemState); }).length, [items, pack, language]);
  function updateValue(field: FieldName, value: string) { if (!selectedItem) return; setPack((current) => { const previous = current[selectedKey]?.[language] || emptyState(); return { ...current, [selectedKey]: { ...(current[selectedKey] || {}), [language]: { values: { ...previous.values, [field]: value }, reviewed: false, published: false } } }; }); }
  function setReviewed(reviewed: boolean) { if (!selectedItem || (reviewed && !isComplete)) return; setPack((current) => { const previous = current[selectedKey]?.[language] || emptyState(); return { ...current, [selectedKey]: { ...(current[selectedKey] || {}), [language]: { ...previous, reviewed, published: reviewed ? previous.published : false } } }; }); }
  function setPublished(published: boolean) { if (!selectedItem || !isComplete || !state.reviewed) return; setPack((current) => { const previous = current[selectedKey]?.[language] || emptyState(); return { ...current, [selectedKey]: { ...(current[selectedKey] || {}), [language]: { ...previous, published } } }; }); }
  async function saveSelected() { const client = supabase; if (!client || !selectedItem) { setError("اختر عنصراً أولاً."); return; } const active = activeFields(selectedItem); if (!active.some((field) => Boolean(state.values[field.key]?.trim()))) { setError("أدخل ترجمة واحدة على الأقل قبل الحفظ."); return; } if (state.reviewed && !isComplete) { setError("لا يمكن اعتبار الترجمة مراجعة قبل اكتمال كل الحقول العربية المتوفرة."); return; } const reviewed = Boolean(state.reviewed && isComplete); const published = Boolean(state.published && reviewed && isComplete); const status = published ? "published" : reviewed ? "reviewed" : "needs_review"; setSaving(true); setError(""); setMessage(""); const { error: saveError } = await adminBoundaryMutation("pr116_translations_gallery_page_entity_content_translations_upsert", { values: active.map((field) => ({ source_type: "gallery_items", source_id: selectedItem.id, field_name: field.key, language, translated_value: state.values[field.key] || "", status, reviewed, is_published: published, created_by: adminEmail, updated_by: adminEmail, updated_at: new Date().toISOString() })), filters: [], select: undefined, returnMode: "many", options: { onConflict: "source_type,source_id,field_name,language" } }); setSaving(false); if (saveError) { setError(`تعذر حفظ ترجمة عنصر المعرض: ${saveError.message}`); return; } setMessage(published ? "تم حفظ الترجمة ونشرها يدوياً. لن تظهر للعامة إلا إذا كانت البطاقة مكتملة." : reviewed ? "تم حفظ الترجمة بحالة مراجع. لن تظهر للعامة قبل تفعيل النشر اليدوي." : "تم الحفظ بحالة تحتاج مراجعة. لن تظهر للعامة."); await loadContent(); }
  if (checking || loading) return <main dir="rtl" className="min-h-screen bg-[#070009] p-6 text-white"><div className="mx-auto max-w-5xl rounded-3xl border border-white/10 bg-white/[0.04] p-8 text-center text-white/65">جاري تجهيز ترجمة المعرض...</div></main>; if (!authorized) return null;
  return <main dir="rtl" className="min-h-screen bg-[#070009] p-5 pb-24 text-white md:p-8"><section className="mx-auto max-w-7xl"><div className="mb-8 flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><div className="mb-3 inline-flex rounded-full border border-yellow-400/25 bg-yellow-500/10 px-5 py-2 text-sm font-bold text-yellow-100">Gallery translations</div><h1 className="text-4xl font-black md:text-5xl">مراجعة ونشر ترجمات المعرض</h1><p className="mt-3 max-w-3xl leading-8 text-white/60">لا يظهر عنصر Gallery EN/TR للعامة إلا عند اكتمال حقوله المتوفرة ومراجعته ونشره يدوياً.</p></div><div className="flex flex-wrap gap-3"><Link href="/admin/translations/automation" className="rounded-full border border-fuchsia-400/25 bg-fuchsia-500/10 px-5 py-3 font-bold text-fuchsia-100">ترجمة دفعة مراقبة</Link><Link href="/admin/gallery" className="rounded-full border border-white/10 bg-white/[0.04] px-5 py-3 font-bold text-white/75">إدارة المعرض</Link></div></div>{message ? <div className="mb-5 rounded-3xl border border-green-400/25 bg-green-500/10 p-5 leading-8 text-green-100">{message}</div> : null}{error ? <div className="mb-5 rounded-3xl border border-red-400/25 bg-red-500/10 p-5 leading-8 text-red-100">{error}</div> : null}<div className="mb-6 grid gap-4 md:grid-cols-3"><Stat label="إجمالي العناصر" value={items.length} /><Stat label={`مراجعة (${language})`} value={reviewedCount} /><Stat label={`منشورة (${language})`} value={publishedCount} /></div><div className="grid gap-6 lg:grid-cols-[0.8fr_1.5fr]"><aside className="rounded-[2rem] border border-white/10 bg-white/[0.04] p-4"><div className="mb-4 flex gap-2">{languages.map((item) => <button key={item.code} type="button" onClick={() => setLanguage(item.code)} className={`flex-1 rounded-2xl px-4 py-3 font-bold ${language === item.code ? "bg-yellow-500/20 text-yellow-100" : "bg-black/20 text-white/65"}`}>{item.label}</button>)}</div><div className="grid gap-2">{items.map((item) => { const itemState = pack[packKey(item.id)]?.[language] || emptyState(); const percent = progress(item, itemState); return <button key={item.id} type="button" onClick={() => setSelectedId(item.id)} className={`rounded-2xl border p-4 text-right ${selectedItem?.id === item.id ? "border-yellow-300/60 bg-yellow-500/10" : "border-white/10 bg-black/20"}`}><div className="font-black">{item.title}</div><div className="mt-2 flex items-center justify-between text-xs text-white/55"><span>{percent}% مكتمل</span><span>{itemState.published && itemState.reviewed && percent === 100 ? "منشور" : itemState.reviewed && percent === 100 ? "مراجع" : "يحتاج مراجعة"}</span></div></button>; })}{!items.length ? <div className="rounded-2xl border border-dashed border-white/15 p-5 text-center text-white/60">لا توجد عناصر معرض حالياً.</div> : null}</div></aside><section className="rounded-[2rem] border border-white/10 bg-white/[0.04] p-6">{selectedItem ? <><div className="mb-6"><div className="text-sm font-bold text-yellow-100">{language === "en" ? "English" : "Türkçe"}</div><h2 className="mt-2 text-3xl font-black">{selectedItem.title}</h2><p className="mt-3 text-white/60">أكمل الحقول العربية المتوفرة فقط. كل تعديل يعيد الترجمة إلى حالة تحتاج مراجعة.</p></div><div className="grid gap-5">{activeFields(selectedItem).map((field) => <label key={field.key} className="block rounded-3xl border border-white/10 bg-black/20 p-4"><div className="mb-3"><div className="font-black">{field.label}</div><div className="mt-1 text-sm text-white/55">{field.helper}</div></div><div className="mb-3 rounded-2xl border border-white/10 bg-white/[0.04] p-3 text-sm leading-7 text-white/70">{sourceValue(selectedItem, field.key)}</div><textarea value={state.values[field.key] || ""} onChange={(event) => updateValue(field.key, event.target.value)} className="min-h-28 w-full resize-y rounded-2xl border border-white/10 bg-[#070009] p-4 text-white outline-none focus:border-yellow-300/60" /></label>)}</div><div className="mt-6 grid gap-4 rounded-3xl border border-white/10 bg-black/20 p-5 md:grid-cols-2"><label className="flex items-start gap-3"><input type="checkbox" checked={state.reviewed} disabled={!isComplete} onChange={(event) => setReviewed(event.target.checked)} className="mt-1 h-5 w-5 accent-yellow-500 disabled:opacity-50" /><span><strong className="block">تمت المراجعة</strong><span className="mt-1 block text-sm text-white/55">يتطلب اكتمال كل الحقول المتوفرة.</span></span></label><label className="flex items-start gap-3"><input type="checkbox" checked={state.published} disabled={!isComplete || !state.reviewed} onChange={(event) => setPublished(event.target.checked)} className="mt-1 h-5 w-5 accent-green-500 disabled:opacity-50" /><span><strong className="block">نشر يدوي للعامة</strong><span className="mt-1 block text-sm text-white/55">لا يوجد أي نشر تلقائي.</span></span></label></div><button type="button" onClick={() => void saveSelected()} disabled={saving} className="mt-6 rounded-full bg-gradient-to-r from-yellow-500 to-orange-500 px-7 py-4 font-black text-black disabled:opacity-60">{saving ? "جاري الحفظ..." : "حفظ حالة الترجمة"}</button></> : <div className="rounded-3xl border border-dashed border-white/15 p-8 text-center text-white/60">اختر عنصراً لبدء المراجعة.</div>}</section></div></section></main>;
}
function Stat({ label, value }: { label: string; value: number }) { return <div className="rounded-3xl border border-white/10 bg-white/[0.04] p-5"><div className="text-sm text-white/55">{label}</div><div className="mt-2 text-3xl font-black">{value}</div></div>; }
