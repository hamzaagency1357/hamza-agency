"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { requireAdminModuleAccess } from "@/lib/adminAccess";
import { isSupabaseConfigured, supabase } from "@/lib/supabase";

type Check={label:string;status:"healthy"|"warning"|"error";value:string;details?:string};
type ScheduleStatus={available?:boolean;scheduled?:boolean;schedule?:string|null;last_run?:string|null;last_status?:string|null;last_success?:string|null};
export default function SystemHealthPage(){const router=useRouter();const[checks,setChecks]=useState<Check[]>([]);const[loading,setLoading]=useState(true);const[checkedAt,setCheckedAt]=useState("");useEffect(()=>{void(async()=>{const access=await requireAdminModuleAccess("launch_checklist");if(!access.isAuthorized){router.replace(access.reason==="forbidden"?"/admin":"/admin/login");return}await run()})()},[router]);
 async function run(){setLoading(true);const next:Check[]=[];if(!isSupabaseConfigured||!supabase){setChecks([{label:"Supabase",status:"error",value:"غير مهيأ"}]);setLoading(false);return}const start=performance.now();const[db,backups,restore,errors,pending,versions,migrations,schedule]=await Promise.all([supabase.from("pages").select("id",{count:"exact",head:true}),supabase.from("backups").select("status,created_at,completed_at").order("created_at",{ascending:false}).limit(20),supabase.from("restore_operations").select("mode,status,created_at,completed_at").order("created_at",{ascending:false}).limit(10),supabase.from("activity_logs").select("id",{count:"exact",head:true}).eq("outcome","failure").gte("created_at",new Date(Date.now()-604800000).toISOString()),supabase.from("service_requests").select("id",{count:"exact",head:true}).in("status",["new","pending","under_review"]).lt("created_at",new Date(Date.now()-259200000).toISOString()),supabase.from("version_history").select("id",{count:"exact",head:true}),supabase.from("operations_preflight_backups").select("migration_key,created_at,checksum").order("created_at",{ascending:false}).limit(1),supabase.rpc("pr99_backup_schedule_status")]);const latency=Math.round(performance.now()-start);next.push({label:"اتصال قاعدة البيانات",status:db.error?"error":latency>2000?"warning":"healthy",value:db.error?"فشل":`${latency} ms`});const successful=(backups.data||[]).find((row)=>["completed","success","completed_with_warnings"].includes(String(row.status)));const age=successful?Date.now()-new Date(successful.created_at).getTime():Infinity;next.push({label:"آخر Backup ناجح",status:age<604800000?"healthy":age<1209600000?"warning":"error",value:successful?new Date(successful.created_at).toLocaleString("ar"):"غير موجود"});const scheduleData=(schedule.data||{}) as ScheduleStatus;next.push({label:"جدولة النسخ الخاصة",status:schedule.error?"warning":scheduleData.scheduled?"healthy":scheduleData.available?"warning":"warning",value:schedule.error?"غير متاحة قبل تطبيق Migration":scheduleData.scheduled?`scheduled · ${scheduleData.schedule||""}`:scheduleData.available?"available / not scheduled":"unavailable",details:`آخر تشغيل: ${scheduleData.last_run?new Date(scheduleData.last_run).toLocaleString("ar"):"—"} · آخر نجاح: ${scheduleData.last_success?new Date(scheduleData.last_success).toLocaleString("ar"):"—"}`});const lastRestore=restore.data?.[0];next.push({label:"آخر Restore test",status:lastRestore?.status==="completed"?"healthy":"warning",value:lastRestore?`${lastRestore.mode} · ${lastRestore.status}`:"لم ينفذ بعد"});next.push({label:"أخطاء تشغيل آخر 7 أيام",status:(errors.count||0)>10?"error":(errors.count||0)>0?"warning":"healthy",value:String(errors.count||0)},{label:"طلبات خدمات متأخرة",status:(pending.count||0)>0?"warning":"healthy",value:String(pending.count||0)},{label:"إصدارات المحتوى",status:versions.error?"warning":"healthy",value:String(versions.count||0)});const snapshot=migrations.data?.[0];next.push({label:"Migration safety snapshot",status:snapshot?"healthy":"error",value:snapshot?.migration_key||"غير موجود",details:snapshot?.checksum?`SHA-256: ${String(snapshot.checksum).slice(0,16)}…`:undefined},{label:"متغير Supabase URL",status:process.env.NEXT_PUBLIC_SUPABASE_URL?"healthy":"error",value:process.env.NEXT_PUBLIC_SUPABASE_URL?"موجود":"مفقود"},{label:"متغير Supabase public key",status:process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY?"healthy":"error",value:process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY?"موجود":"مفقود"},{label:"Commit SHA",status:process.env.NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA?"healthy":"warning",value:process.env.NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA?.slice(0,12)||"غير متاح محلياً"});setChecks(next);setCheckedAt(new Date().toLocaleString("ar"));setLoading(false)}
 return <main dir="rtl" className="min-h-screen bg-[#070009] p-5 pb-36 text-white md:p-8"><section className="mx-auto max-w-6xl"><header className="mb-7 flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><span className="rounded-full border border-cyan-400/30 bg-cyan-500/10 px-4 py-2 text-sm font-black text-cyan-100">Operations</span><h1 className="mt-4 text-4xl font-black">حالة النظام</h1><p className="mt-2 text-white/50">فحوص آمنة لا تعرض قيم الأسرار. آخر تحديث: {checkedAt||"—"}</p></div><div className="flex gap-2"><button onClick={()=>void run()} disabled={loading} className="rounded-full bg-cyan-600 px-5 py-3 font-black disabled:opacity-50">{loading?"جارٍ الفحص...":"إعادة الفحص"}</button><Link href="/admin" className="rounded-full border border-white/15 px-5 py-3 font-bold">لوحة الإدارة</Link></div></header><div className="grid gap-4 md:grid-cols-2">{checks.map((check)=><article key={check.label} className={`rounded-3xl border p-5 ${check.status==="healthy"?"border-green-400/20 bg-green-500/10":check.status==="warning"?"border-yellow-400/20 bg-yellow-500/10":"border-red-400/20 bg-red-500/10"}`}><div className="flex items-center justify-between gap-3"><h2 className="font-black">{check.label}</h2><span className="rounded-full border border-white/10 bg-black/20 px-3 py-1 text-xs">{check.status}</span></div><div className="mt-3 text-2xl font-black">{check.value}</div>{check.details&&<p className="mt-3 break-all text-xs text-white/45">{check.details}</p>}</article>)}</div></section></main>}
